---
# litedump_databases / litedump_retention / litedump_timer_oncalendar come from
# molecule.yml group_vars (single source of truth, also visible to prepare + verify).
- name: Converge
  hosts: all
  become: true
  roles:
    - role: eyebrowkang.litedump
